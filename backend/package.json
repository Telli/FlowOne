{
  "name": "flowone-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn backend.app:app --reload --port 8000",
    "start": "uvicorn backend.app:app --host 0.0.0.0 --port 8000"
  }
}

